AI-marked text extracted from Turnitin PDF

Source file: Test file (2)-1.pdf
AI writing overview in PDF: 43% detected as AI
Scope: cyan-highlighted passages in the thesis submission only.

[1] Report page 7
Addressing these challenges is crucial for maintaining the confidentiality and integrity of sensitive application components in modern mobile ecosystems.

[2] Report page 7
Problem of the thesis. Existing protection mechanisms do not address scenarios where native libraries load other native libraries, leaving such cases vulnerable to unauthorized access. This thesis proposes and implements a proof-of-concept solution that intercepts Android native library loading calls and replaces them with custom logic to decrypt libraries before they are loaded, thereby extending protection beyond standard Java-based loading mechanisms.

[3] Report page 7
Objective of the thesis: Research possible solutions for intercepting Android native loading calls and developing a proof-of-concept implementation.

[4] Report page 7
Tasks of the thesis:

[5] Report page 7
Analyze native library loading mechanisms in Android.

[6] Report page 7
Examine and evaluate techniques for intercepting native loading calls.

[7] Report page 7
Design a proof-of-concept architecture for custom interception and decryption.

[8] Report page 7
Implement and test the proof-of-concept solution.

[9] Report page 8
Android is a mobile operating system that provides the fundamental environment for applications and system services. It serves as the platform for hardware manufacturers and software developers alike to build devices and applications. The system manages hardware components, memory, processes, and security, offering a structured and controlled environment for application execution. Application development on this platform is primarily carried out using the Android Software Development Kit (SDK). The SDK provides a comprehensive set of tools, libraries, and programming interfaces designed to facilitate the creation of user interfaces, data handling mechanisms, and interactions with system services. It enables developers to build complete applications in high-level languages while maintaining compatibility across different devices and Android versions.

[10] Report page 8
In addition to the SDK, the Android Native Development Kit (NDK) allows developers to integrate components written in the native languages of the Android operating system. This toolkit is commonly used for implementing performance-critical operations or reusing existing native code libraries. The NDK operates closer to the hardware layer and provides capabilities for extending application functionality beyond the managed environment offered by the SDK.

[11] Report page 8
Another concept relevant to this thesis is hooking, also referred to as function interception. Hooking represents a method for observing or modifying the behavior of software components during execution. Developers can use it to monitor program activity, debug system behavior, or extend functionality without directly altering the original codebase. However, these same mechanisms can be exploited by attackers to manipulate application logic, intercept sensitive operations, or bypass security protections. Therefore, function interception techniques play a dual role in software engineering and security research.

[12] Report page 8
A final key concept is encryption and decryption of encrypted libraries. Encryption is the process of converting readable information into an unreadable format to ensure confidentiality and protect data from unauthorized access. Decryption is the reverse process that restores the original information when proper authorization is provided. In the context of Android applications, encryption is often applied to secure application data, communication channels, or even executable components, such as native libraries. When combined with controlled interception mechanisms, encryption ensures that sensitive content remains protected until it is safely decrypted at runtime by trusted components.

[13] Report page 10
Native code interacts with the managed environment through the Java Native Interface (JNI). JNI is a standardized framework that enables interoperability between managed Java code and unmanaged native code written in C or C++. It allows Java applications to invoke native functions for performance critical tasks, system level operations, or to reuse existing native codebases. It also permits native code to call back into Java, creating a bidirectional communication channel between the Android Runtime (ART) and compiled native libraries.

[14] Report page 10
A typical JNI workflow is illustrated in fig. 2. At a high level, the application declares one or more methods as native in the Java or Kotlin layer, requests loading of the corresponding shared library via System.loadLibrary(), and relies on ART and JNI to bind these declarations to concrete functions in the native library. After the library is loaded, calls from managed code to the native methods are routed through the JNI bridge to the compiled C or C++ implementation, which can then interact with Java objects through the JNIEnv* interface and return data back to the managed layer.

[15] Report page 10
High level JNI workflow between Java and native layers.

[16] Report page 10
The JNIEnv* interface exposed to native code provides access to the managed environment, including operations such as creating new Java strings, invoking Java methods, or managing exceptions. JNI therefore

[17] Report page 11
Android native libraries are distributed as shared objects with the .so extension.

[18] Report page 11
During loading, the dynamic linker updates GOT entries so that subsequent calls jump directly to the correct target function. Because this indirection is based on tables of function pointers, native hooking techniques often modify or replace those resolved entries after load in order to redirect execution flow [4].

[19] Report page 12
The ELF format also encodes library dependencies using entries in the DT_NEEDED list. Each DT_NEEDED record names another shared object that must be available for correct execution. When a library is loaded, the Android dynamic linker inspects these entries, locates the required dependencies, and loads them into memory if they are not already present. The linker then performs relocations, which are adjustments that patch absolute and relative addresses in code and data so that all symbol references point to the correct locations in the current process image. After relocations are applied, external calls through the PLT and GOT resolve to valid function addresses [3].

[20] Report page 12
After mapping segments and applying relocations, the loader executes any registered initialization routines.

[21] Report page 13
ART then records the handle of the loaded library and associates it with the calling ClassLoader for future lookups.

[22] Report page 13
In addition to Java-based loading, libraries can also be loaded directly from native code using dlopen() without going through System.loadLibrary(). In that case, the library is opened by the dynamic linker from C/C++ code, and the Java side may never see it as part of a ClassLoader. This pattern is widely used in engine-style code and multilayer runtimes, where one native component loads and binds another at runtime using only the POSIX-level dynamic loading API.

[23] Report page 14
Android application native library loading flow.

[24] Report page 15
Java-level hooking focuses on intercepting method calls at the managed code layer. It targets Android framework or application methods that execute in ART or a Java Virtual Machine (JVM)-like environment [2].

[25] Report page 15
One practical approach is method replacement through reflection. Using the java.lang.reflect API, private or system methods can be accessed, their accessibility modified, and their behavior overridden at runtime. This enables selective interception without repackaging the application.

[26] Report page 15
Instrumentation frameworks such as Xposed [6] load a hook manager early in process startup and register handlers that run before or after targeted Java methods. In practice, a modified Zygote injects the Xposed bridge so modules can intercept framework and application APIs, substitute return values, and record arguments. Research systems extend this model to configurable monitors (e.g., Droidmon/ARTmon) that hook framework APIs and even mask environment fingerprints during analysis [7]. Java-layer tools like Frida [8] can also attach to a running process and modify Java method behavior dynamically without prior application modification.

[27] Report page 15
Limitations — Java hooks do not see native-only execution. They cannot directly intercept native library loading through dlopen() or calls resolved purely in the dynamic linker. Anti-tamper checks may also detect injected managers or altered method paths, and modern policies constrain deployment on stock devices.

[28] Report page 15
On Android, traditional LD_PRELOAD is constrained by linker namespaces and application sandboxing. Effective interposition often relies on early injection, custom loader hooks, or process-specific load order control [4]. Research frameworks combine Java-layer hooks with operating system-level controls to reduce detectability while observing cross-layer behavior [7].

[29] Report page 15
Intercepting native loading. Hooking dlopen() and related loader entry points allows observing,

[30] Report page 16
This is essential when decrypting or unpacking protected libraries before use. Implementations must account for Android linker behavior, namespaces, and ART interactions [4, 5].

[31] Report page 16
Protections and detection. Effective native hooks must handle Address Space Layout Randomization (ASLR), Security-Enhanced Linux (SELinux) constraints, and application integrity checks. Many applications verify code sections or loader state and may probe for modified tables or suspicious mappings. Analysis systems mitigate this by minimizing overhead, targeting hooks precisely, and correlating framework- and operating system-level signals [7, 4].

[32] Report page 16
Inline hooking modifies instructions at the beginning of a function in order to redirect control flow to a custom handler before optionally returning to the original implementation. The typical pattern is to overwrite the function prologue with a branch instruction that jumps to a trampoline, which saves register state, executes custom logic, and then either calls the original function body or bypasses it completely. Because the overwritten instructions are lost when the branch is written, they must be relocated into the trampoline in a way that preserves original semantics, including correct handling of relative addresses and literal pools [4].

[33] Report page 16
On Android, inline hooks must account for the instruction set and calling conventions of the underlying CPU architecture. Historically, devices have used both ARM and Thumb instruction sets for 32 bit binaries, while modern devices predominantly use AArch64. Hooking frameworks therefore need to detect the current instruction mode, choose an appropriate branch encoding, and ensure that the trampoline is correctly aligned and resides in executable memory [4, 7]. In environments with Address Space Layout Randomization (ASLR), the framework must compute target addresses at runtime rather than relying on fixed offsets.

[34] Report page 16
Inline hooking has the advantage that it can target functions even when they are not exported through the dynamic symbol table. As long as the function address is known, either from static analysis or from in process symbol resolution, its prologue can be patched. This makes inline hooks suitable for intercepting internal helper functions or decryption routines that are deliberately hidden from the dynamic linker.

[35] Report page 17
In contrast to inline patching, PLT based hooks operate entirely on data structures that the dynamic linker already manages. They do not require rewriting instructions in executable segments, which improves robustness across compiler optimizations and reduces the risk of violating code integrity checks. Hook implementations must still locate the relevant relocation entries, identify which GOT slots correspond to the targeted symbol, and update them atomically to avoid race conditions.

[36] Report page 18
Once attached, the hook receives callbacks on every function entry and exit, with access to register values and arguments. The script can then log or modify parameters, alter return values, or redirect control flow, which effectively implements inline hooks without manual patching of instruction bytes [4].

[37] Report page 18
Because Frida can observe both Java and native layers in the same process, it is particularly suitable for studying cross layer behavior such as native library loading. A typical pattern is to hook Java calls that request loading of a library and, in parallel, intercept the corresponding native dlopen() and dlsym() calls. This allows the analyst to correlate library names, file system paths, and resolved symbol addresses, and to verify when a protected library is mapped into memory. In scenarios where libraries are encrypted or unpacked at runtime, Frida scripts can target the decryption routines directly, dump decrypted code from memory, or delay execution until specific conditions are met [4, 7].

[38] Report page 18
Despite its flexibility, Frida is primarily designed as an analysis and instrumentation tool rather than a production protection mechanism.

[39] Report page 19
LSPosed also integrates an online repository of modules, making it easier to discover and update functionality such as ad blocking, UI customization, or privacy enhancements directly from the manager interface [11, 12].

[40] Report page 19
Platform coverage and limitations. LSPosed is designed specifically for modern Android releases and supports versions from Android 8.1 up to Android 16 according to recent stable releases [10, 11, 12].

[41] Report page 20
A proxy function can validate requested paths, enforce policy, or transparently decrypt protected libraries before forwarding the call to the original loader routine. Because ByteHook is capable of hooking all callers of a given symbol across all dynamic libraries and automatically tracking newly loaded modules, it can provide comprehensive coverage for native loading flows in complex applications that rely on multiple runtime environments and engine style code.[14, 9]

[42] Report page 20
1.4 Summary of relevant technologies

[43] Report page 20
This section has outlined the key technical foundations required for the proposed protection system. The Android platform was introduced as a layered software stack built on a modified Linux kernel, with native libraries packaged as ELF shared objects and exposed to applications through the Android Runtime (ART) and the Java Native Interface (JNI).

[44] Report page 22
For libraries that are not in the protected list, the proxy forwards the loading request directly to the original dynamic linker implementation without modification. This design preserves normal application behavior for unprotected components while selectively applying decryption only where needed.

[45] Report page 22
The scope of the system is limited to in process protection for a single Android application. The design assumes that the application can be rebuilt in order to add the build time encryption step and to link against the native protection library. The system does not attempt to provide system wide protection, cross process enforcement, or integration with hardware backed key storage.

[46] Report page 24
No x86 or x86_64 builds are included in the current prototype.

[47] Report page 24
From the application perspective, the system is implemented as a proof of concept within a dedicated demo application that bundles both the protection library and the protected native libraries. The long term goal is to provide the protection logic as a reusable native library that third party developers could link into their own applications and configure for their specific set of protected libraries. For the purposes of this thesis, the deployment model is restricted to a single application that can be rebuilt as needed to integrate the build time encryption step and the runtime protection component.

[48] Report page 24
The build and development toolchain used for the system consists of Android Studio with Gradle for project management, the Android NDK with CMake for compiling native C/C++ code, and Python for implementing the proof of concept encryption tool that processes compiled shared objects during the build. The design assumes that the application is packaged in the standard way for native libraries and that the manifest is configured with android:extractNativeLibs="true" so that the operating system extracts shared libraries to the application specific /data/app/.../lib/<abi>/ directory.

[49] Report page 27
Security related requirements define that no valid protected libraries shall exist on disk, that decrypted content shall remain confined to process memory with a controlled lifetime, and that the core mechanism shall remain independent of any specific encryption algorithm so that key management and cryptographic strength can be improved without redesigning the interception and loading logic.

[50] Report page 28
This subsection defines the internal structure of the proof-of-concept system and clarifies how the responsibilities are divided across the subsequent implementation subsections. The architecture is intentionally split into build-time and runtime stages: build-time ensures that protected libraries are not valid ELF binaries on disk, while runtime restores validity only inside the target process immediately before loading.

[51] Report page 28
The system is composed of the following logical components.

[52] Report page 28
Build-time encryption tool (Python).

[53] Report page 29
Routing decision for each load request. Every intercepted request is normalized (handling simple names and path-like inputs) and checked against the protected library registry. If the library is not protected, the hook forwards the request to the original loader function without modification, keeping the behavior consistent for unprotected dependencies.

[54] Report page 29
Decryption and in-memory load for protected libraries. If the registry marks the request as protected, the system resolves the on-disk location of the encrypted file (assuming standard extraction paths), reads it, decrypts it into memory, and validates that the result is a plausible ELF shared object. The decrypted bytes are then written into an anonymous in-memory file descriptor created via memfd_create. Finally, the library is loaded by calling android_dlopen_ext with ANDROID_DLEXT_USE_LIBRARY_FD, so the linker consumes the in-memory representation rather than a plaintext file on disk.

[55] Report page 29
Post-load usage and nested loading coverage. After a successful load, the returned handle is used normally (e.g., via dlsym).

[56] Report page 30
The choice of XOR encryption for this proof of concept is intentional: it demonstrates the complete protection mechanism with minimal complexity and ensures that the focus remains on the interception, routing, and loading architecture rather than on cryptographic implementation details. A single byte key is used throughout the system and is stored as a constant in both the encryption script and the native decryption logic, which simplifies the proof of concept but represents a clear limitation for production use.

[57] Report page 30
The encryption process is straightforward. The script accepts the path to a compiled .so file, reads its entire content into memory as a byte array, iterates through each byte applying the XOR operation with the encryption key, and writes the result back to the same file path, replacing the original. This in place replacement ensures that the build output directory contains only the encrypted version of the library, which is then packaged directly into the APK without further intervention.

[58] Report page 30
The key technical outcome of this operation is that the encrypted file is no longer a valid ELF binary. Before encryption, a shared library begins with the standard ELF magic bytes 7f 45 4c 46, which identify the file as an executable or linkable format binary to the operating system and the dynamic linker. After the XOR operation, these bytes are transformed into a different sequence that does not correspond to any recognized file format. As a result, if the Android dynamic linker attempts to load the encrypted file directly, it will reject the file with an error indicating that the format is not recognized. This behavior is essential to the protection mechanism, as it ensures that protected libraries cannot be used unless the runtime protection component decrypts them first.

[59] Report pages 30-31
The script is configured to encrypt liblibb.so, which serves as the demonstration case for protected native to native loading in this proof of concept. The library name is hardcoded in the script for simplicity. In a production deployment, the set of libraries to be encrypted should be specified through a configuration file or build parameter to allow flexibility without modifying the encryption tool itself.

[60] Report page 31
To make encryption a reliable and repeatable part of the build pipeline, the encryption script is executed automatically as a post-build step in the native build configuration. In the demo application, this is achieved by attaching a custom CMake command to the libb target, so the encryption runs immediately after the shared object is produced and before the Android packaging phase begins.

[61] Report page 31
The command is configured to execute after the target has been fully linked, with its working directory set to the directory where the compiled liblibb.so artifact is emitted. This allows the script to locate the output library deterministically without hardcoding absolute paths, which keeps the setup portable across different build environments and host operating systems.

[62] Report page 31
Because the script encrypts the library in place, the build output directory contains only the encrypted version of liblibb.so after this step completes. When the Gradle packaging stage later collects native artifacts from the CMake output directories and places them under lib/<abi>/ inside the APK, it automatically includes the already encrypted library.

[63] Report pages 31-32
The native protection core implements this pattern by defining an initialization function named init_hook() with the constructor attribute. The function is declared as static to limit its visibility to the translation unit and is defined in the nativecallhook.cpp source file. When the libnativecallhook.so library is loaded through System.loadLibrary("nativecallhook") in the Java layer, the Android dynamic linker first maps the library into memory, applies relocations, and then invokes init_hook() as part of the standard initialization sequence. Only after this initialization completes does control return to the Java virtual machine, at which point the hook is already installed and operational.

[64] Report page 32
The init_hook() function is responsible for initializing the ByteHook framework and installing the hook on the target loader function. ByteHook is a production grade PLT and GOT hooking library developed by ByteDance that provides a stable API for intercepting dynamically linked function calls in Android applications. The framework handles the low level details of locating PLT entries, modifying GOT slots, and managing hook call stacks across multiple threads.

[65] Report page 32
The first step in the initialization process is to configure the ByteHook library itself. This is accomplished by calling bytehook_init() with two parameters: the hooking mode and a debuggability flag.

[66] Report page 33
If the call fails and returns null, the constructor logs an error and proceeds without an active hook, which leaves protected libraries unloadable but does not crash the application.

[67] Report page 33
3.4 Protected library registry and routing logic

[68] Report page 33
Once the hook is installed, every native library loading request passes through the interception callback. The system determines whether a requested library requires decryption by consulting a registry of protected library identifiers.

[69] Report page 33
The protected library registry is a static constant array named ENCRYPTED_LIBS defined in nativecallhook.cpp. For this proof of concept, it contains a single entry: "liblibb.so". The hardcoded array approach provides simplicity and clarity but requires recompilation to add or remove protected libraries.

[70] Report page 33
The hooked_dlopen() callback serves as the routing point for all loading requests. It performs a lookup using is_encrypted_library(), which uses strstr() to match the requested filename against registry entries. This substring matching works for simple names, absolute paths, or relative paths.

[71] Report page 33
If the lookup returns true, the callback resolves the full filesystem path using get_library_full_path() and invokes load_decrypted_library(). If false, it forwards the request to the original loader using BYTEHOOK_CALL_PREV, ensuring transparent passthrough for unprotected libraries with minimal overhead.

[72] Report page 33
The path resolution assumes android:extractNativeLibs="true" and uses dladdr() to locate the extracted library directory. This works reliably when all libraries are installed in the same directory.

[73] Report page 33
The main limitation is the hardcoded registry, which prevents runtime configuration. Production systems should use a configuration file or dynamic registration through JNI. Additionally, the registry stores only filenames without metadata such as hashes or key identifiers, and the substring matching could produce false positives in edge cases.

[74] Report page 33
3.5 In-memory decryption and loading

[75] Report page 33
When a protected library is identified through the registry lookup, the system must decrypt it and load it into the process without writing the plaintext to persistent storage. This is accomplished through the load_decrypted_library() function, which implements a five step workflow: reading the encrypted file, decrypting in memory, validating the result, creating an anonymous memory file descriptor, and loading through the Android dynamic linker.

[76] Report page 34
Reading and decrypting the encrypted library

[77] Report page 34
The function begins by reading the encrypted library file from disk into a memory buffer. The file path is resolved by get_library_full_path() and points to the extracted library directory under /data/app/. The entire file is read as a byte array using standard C++ file I/O operations.

[78] Report page 34
Once loaded into memory, the decryption process applies the XOR cipher to every byte in the buffer. The implementation iterates through the byte array and performs the XOR operation with the encryption key. Because XOR is symmetric, applying the same operation with the same key reverses the encryption performed at build time.

[79] Report page 34
After decryption, the system verifies that the result is a valid ELF binary by checking the magic bytes at the start of the buffer. A valid ELF file must begin with the four byte sequence 7f 45 4c 46 (the ASCII characters DEL, ’E’, ’L’, ’F’). If these bytes are not present after decryption, the function concludes that either the wrong decryption key was used, the encrypted file is corrupted, or the file was tampered with. In any of these cases, the function logs an error and returns null without attempting to load the library. This validation enforces the fail safe requirement that invalid or incorrectly decrypted libraries must not execute.

[80] Report page 34
Anonymous file descriptor creation with memfd_create

[81] Report page 34
With a validated decrypted buffer in memory, the next step is to create an in memory file that can be passed to the dynamic linker. This is accomplished using the memfd_create() system call, which creates an anonymous file descriptor backed by RAM rather than persistent storage. The descriptor behaves like a regular file handle for read and write operations but exists only in memory and is automatically released when the process exits or closes the descriptor.

[82] Report page 34
The system invokes memfd_create() with a name argument for debugging purposes and the MFD_CLOEXEC flag to ensure the descriptor is closed on process execution. If the call fails, which may occur on systems with insufficient memory or restricted system call access, the function logs the error and returns null.

[83] Report page 34
Once the descriptor is created, the system resizes it to match the size of the decrypted buffer using ftruncate() and writes the entire decrypted ELF content to the descriptor using the write() system call. After writing, the file position is reset to the beginning, leaving the descriptor ready to be used as an input file for loading.

[84] Report page 34
The final step is to load the library from the memory file descriptor into the process address space. This is accomplished using the Android specific function android_dlopen_ext(), which extends the standard dlopen() interface with additional options for controlling library loading behavior.

[85] Report page 34
The function requires an android_dlextinfo structure that specifies the loading mode. For this

[86] Report page 35
This flag instructs the linker to read the library content from the provided file descriptor rather than opening a file by path. The linker maps the ELF segments from the descriptor into memory, applies relocations, resolves dependencies, executes initialization functions, and returns a library handle that can be used with dlsym() and other dynamic linking APIs.

[87] Report page 35
If loading succeeds, the function retains the memfd descriptor for the lifetime of the library to prevent premature deallocation of the backing memory. If loading fails, the descriptor is closed and null is returned to the caller. In both cases, the decrypted buffer is discarded after being written to the descriptor, ensuring that no additional plaintext copies remain in process memory beyond what the linker has already mapped.

[88] Report page 35
This approach ensures that the decrypted library exists only in process memory and is never written to persistent storage in plaintext form. The encrypted file on disk remains invalid and unloadable by standard tools. An attacker with filesystem access can retrieve only the encrypted version, which requires knowledge of the decryption key to use.

[89] Report page 35
However, the decrypted content is present in the process address space once loaded and could be extracted by an attacker with sufficient privileges to read process memory or attach a debugger. The system does not implement anti debugging or memory protection mechanisms beyond the standard operating system process isolation. These limitations are acceptable for the proof of concept but would need to be addressed in a production system through techniques such as code obfuscation, anti debugging checks, or hardware backed attestation.

[90] Report page 35
3.6 Java-Native integration and load order

[91] Report page 35
The protection mechanism requires correct loading order to ensure hooks are installed before any protected libraries are requested. This is enforced through Java static blocks and a JNI bridge.

[92] Report page 35
The NativeCallHook Java class provides a static block that loads libnativecallhook.so via System.loadLibrary(), triggering the C++ constructor that installs hooks. The MainActivity static block enforces the critical sequence: first call NativeCallHook.initialize() to load the protection library, then call System.loadLibrary("liba") to load application libraries. This guarantees hooks are active before any protected library load attempts.
